// реализаци€ пользовательского приложени€


#include<string>
#include "Polynom.h"
using namespace std;


void main()
{
	/*int k = 0, l;
	double c;
	do {
		cout << "Choose one of the items:" << '\n';
		string d[3] = { "1. Input Polynomials","2. Exit" };
		for (int i = 0; i < 2; i++)
			cout << d[i] << '\n';
		cout << "Input a menu number:";
		cin >> k;
		if (k == 2) break;
		if (k == 1) {
			Polynom A, B, C;
			cout << "Input polynom A" << '\n';
			cin >> A;
			cout << "Input polynom B" << '\n';
			cin >> B;
			l = 0;
			while (l<7) {
				string e[7] = { "1. A+B=","2. A-B=","3. B-A=", "4. A*B=","5. A*c=","6. B*c=", "7. Exit" };
				for (int i = 0; i < 7; i++)
					cout << e[i] << '\n';
				cout << "Input a menu number:";
				cin >> l;
				switch (l) {
				case 1: {
					C = A + B;
					cout << C << '\n';
					break;
				}
				case 2: {
					C = A - B;
					cout << C << '\n';
					break;
				}
				case 3: {
					C = B - A;
					cout << C << '\n';
					break;
				}
				case 4: {
					C = A * B;
					cout << C << '\n';
					break;
				}
				case 5: {
					cout << "Input a const: ";
					cin >> c;
					C = A * c;
					cout << C << '\n';
					break;
				}
				case 6: {
					cout << "Input a const: ";
					cin >> c;
					C = B * c;
					cout << C << '\n';
					break;
				}
				}
			}
		}
	} while (k < 2);*/
}